apiVersion: apps/v1
kind: Deployment
metadata:
  name: owner-service
  namespace: team02
spec:
  replicas: 1
  selector:
    matchLabels:
      app: owner-service
  template:
    metadata:
      labels:
        app: owner-service
    spec:
      containers:
      - name: owner-service
        image: docker.io/wsk221e/team02-owner-service:latest
        ports:
        - containerPort: 8081
        envFrom:
          - secretRef:
              name: app-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: owner-service
  namespace: team02
spec:
  type: ClusterIP
  selector:
    app: owner-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP

