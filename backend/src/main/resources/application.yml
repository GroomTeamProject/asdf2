spring:
  application:
    name: team02core

  datasource:
    url: jdbc:mariadb://localhost:3306/quickdeliver?serverTimezone=UTC
    username: root
    password: Wnsrns3224!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  flyway:
    enabled: false

cors:
  allowed-origins: "*"
