spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/quickdeliver
    username: root
    password: Wnsrns3224!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 테이블 없으면 자동 생성
    show-sql: true      # 콘솔에 SQL 찍힘
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false   # MariaDB 12 호환 문제로 Flyway 비활성화

toss:
  secretKey: test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm

